<table class="table table-striped table-bordered table-condensed">
        
    <thead>
        <tr>
            <th><input type="checkbox" name="" /></th>
            <th>id</th>
            <th>Libellé</th>
            <th>action</th>
        </tr>
    </thead>
    <tbody>
        {% for cat in items %} 
            <tr>
                <td><input type="checkbox" name="" /></td>
                <td>{{cat.idCategorie}}</td>
                <td>{{cat.libelleCategorie}}</td>
                <td><a href="{{ path('appolo_back_office_get', {'type': 'categorie','id' : cat.idCategorie }) }}">editer</a> |
                    <a href="{{ path('appolo_back_office_delete', {'type': 'categorie','id' : cat.idCategorie }) }}">supprimer</a></td>
            </tr>
        {% endfor %}
        
    </tbody>
</table>
