<table class="table table-striped table-bordered table-condensed">
        
    <thead>
        <tr>
            <th><input type="checkbox" name="" /></th>
            <th>id</th>
            <th>libellé</th>
            <th>Action</th>
        </tr>
    </thead>
    <tbody>
        {% for role in items %} 
            <tr>
                <td><input type="checkbox" name="" /></td>
                <td>{{role.idRoleUs}}</td>
                <td>{{role.libelleRoleUs}}</td>
                <td><a href="{{ path('appolo_back_office_get', {'type': 'categorie','id' : role.idRoleUs }) }}">editer</a> |
                    <a href="{{ path('appolo_back_office_delete', {'type': 'categorie','id' : role.idRoleUs }) }}">supprimer</a></td>
            </tr>
        {% endfor %}
    </tbody>
</table>
</div>
